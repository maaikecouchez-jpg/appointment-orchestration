// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Deployed to GitHub Pages
  site: 'https://maaikecouchez-jpg.github.io',
  base: '/appointment-orchestration',
});
